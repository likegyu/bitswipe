'use client';

import React from 'react';

export const Footer = () => {
    return (
        <footer className="w-full p-2 sm:p-4 text-center text-xs text-gray-400" style={{ paddingBottom: 'max(0.5rem, env(safe-area-inset-bottom))' }}>
            <p>© 2025 Bitswipe. This is a simulation game. No real money involved.</p>
        </footer>
    );
};
